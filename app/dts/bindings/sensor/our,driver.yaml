description: Our simple LED sensor driver.
compatible: "our,driver"
include: [base.yaml]
properties:
  gpios:
    type: phandle-array
    required: true
    description: GPIO pin connected to the on-board LED.
